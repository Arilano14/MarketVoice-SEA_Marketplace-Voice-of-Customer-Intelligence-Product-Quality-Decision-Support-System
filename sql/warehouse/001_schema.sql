-- DEL-08 Physical DDL (Phase 6 v1.1)
-- 001_schema.sql
-- Scope: Schema only. No objects, no constraints, no views, no Track B.
-- Track A (authentic) MVP only. No DEL-11 marts (Phase 7 only).

CREATE SCHEMA IF NOT EXISTS marketvoice_warehouse;
