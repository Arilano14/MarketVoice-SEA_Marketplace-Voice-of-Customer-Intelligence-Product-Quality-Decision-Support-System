project:
  name: "MarketVoice SEA"
  full_title: "Marketplace Voice-of-Customer Intelligence & Product Quality Decision Support System"
  version: "0.1.0"
  phase: "Phase 5 - Solution Architecture & Logical Data Model"
  phase_status_note: "Phases 0-5 governance, source acquisition, forensic audit, requirements, methodology, and architecture artifacts completed. Gate status recorded in docs/governance/phase_gates.md is the canonical source of truth. Phase 6 execution = NOT_STARTED pending separate explicit authorization."

paths:
  data_raw: "data/raw"
  data_interim: "data/interim"
  data_processed: "data/processed"
  data_metadata: "data/metadata"
  logs: "logs"

environment:
  default_seed: 42
  log_level: "INFO"
